import styled from "@emotion/styled";
import {Box} from "@chakra-ui/react";

const HeaderContainer = styled(Box)`
  & {
     border-bottom: 2px solid #cecaca;
     box-shadow: 1px 1px 5px 1px #cecaca;
  }
`;

export {HeaderContainer};